---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: ephemeralenvs.apps.myrepo.dev
spec:
  group: apps.myrepo.dev
  names:
    kind: EphemeralEnv
    listKind: EphemeralEnvList
    plural: ephemeralenvs
    singular: ephemeralenv
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .status.expiresAt
      name: Expires At
      type: string
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EphemeralEnv is the Schema for the ephemeralenvs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EphemeralEnvSpec defines the desired state of EphemeralEnv
            properties:
              durationMinutes:
                description: DurationMinutes is the lifetime of the environment in
                  minutes before deletion.
                format: int32
                maximum: 1440
                minimum: 1
                type: integer
              image:
                description: Image is the container image to run (e.g., nginx:alpine).
                minLength: 1
                type: string
              port:
                description: Port is the container port on which the workload listens.
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              replicas:
                default: 1
                description: Replicas is the desired replica count for the underlying
                  workload deployment.
                format: int32
                minimum: 1
                type: integer
            required:
            - durationMinutes
            - image
            - port
            type: object
          status:
            description: EphemeralEnvStatus defines the observed state of EphemeralEnv
            properties:
              expiresAt:
                description: |-
                  ExpiresAt is the timestamp calculated at creation time indicating when
                  the environment expires (creation/initialization time + durationMinutes).
                format: date-time
                type: string
              phase:
                description: 'Phase is the current lifecycle phase: Initializing,
                  Active, or Expired.'
                type: string
              readyReplicas:
                description: ReadyReplicas is the number of ready pods reported by
                  the underlying Deployment.
                format: int32
                type: integer
              serviceURL:
                description: ServiceURL is the cluster-internal endpoint string for
                  the managed Service.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
